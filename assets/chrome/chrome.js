// chrome://scripts/chrome.js
//
// Stub script for FastRender internal "chrome HTML" experiments.
// It exists so `<script src="chrome://scripts/chrome.js">` loads without error.

(function () {
  // Intentionally empty for now.
})();

