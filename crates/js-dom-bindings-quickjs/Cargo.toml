[package]
name = "js-dom-bindings-quickjs"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
fastrender = { path = "../.." }
rquickjs = { version = "0.9", features = ["classes", "macro"] }
