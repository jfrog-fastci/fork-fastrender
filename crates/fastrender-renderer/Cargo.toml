[package]
name = "fastrender-renderer"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bincode = "1.3"
fastrender-ipc = { path = "../fastrender-ipc" }
serde = { version = "1", features = ["derive"] }

