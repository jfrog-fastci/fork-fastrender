# Browser chrome (`browser_chrome`)

This workstream owns user-facing **browser chrome**: tabs, navigation, address bar, history UI, menus, shortcuts, and non-page UI state.

Most chrome work lives under `src/ui/` (feature-gated behind `browser_ui`).

