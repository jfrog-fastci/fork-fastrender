# Browser page interaction (`browser_interaction`)

This workstream owns **interactive page behavior**: focus, form controls, scrolling, selection, pointer/keyboard event routing, and interaction-model correctness.

Relevant code lives under `src/interaction/` and the browser UI worker (`src/ui/render_worker.rs`).

