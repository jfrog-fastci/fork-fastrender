//! Layout integration tests (public API only).
//!
//! Note: we keep these in their own test binary (rather than the unified `integration` target) so
//! the suite can be compiled/run independently.

mod layout;

