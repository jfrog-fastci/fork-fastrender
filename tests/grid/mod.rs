//! Grid integration tests.
//!
//! Grid-specific integration tests currently live under other modules (e.g. `tests/layout/` and
//! fixture/WPT coverage). This module is a placeholder so `tests/integration.rs` can include `mod
//! grid;` without requiring a separate integration test binary.
