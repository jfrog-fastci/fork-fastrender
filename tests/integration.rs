//! Unified integration test binary.
//!
//! This crate is intended to replace the many `*_tests.rs` aggregators in `tests/` by collecting
//! public API integration coverage under a single `cargo test --test integration` target.

mod api;

