//! QuickJS fetch binding tests.
//!
//! This module is only built when the `quickjs` feature is enabled.
//!
//! Note: the QuickJS fetch API surface is primarily validated via unit tests in
//! `src/js/legacy/quickjs/fetch.rs`. This file exists so the `tests/misc` harness' module list stays
//! consistent (and so `cargo fmt` can resolve the module).

// Intentionally empty: coverage lives in the unit tests for the bindings.

