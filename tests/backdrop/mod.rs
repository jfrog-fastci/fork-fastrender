//! Backdrop filter-related test modules.
//!
//! Historically this directory hosted backdrop regressions; most have since been promoted to
//! standalone `backdrop_root_*_test.rs` targets. Keep the module in place so the
//! `tests/backdrop_tests.rs` harness remains a valid integration test crate.

