# Core ECMAScript JSON APIs.

schema_version: 1
symbols:
  - name: JSON.parse
    aliases: []
    semantics: JsonParse
    signature: "(text) => any"
    since: "baseline"
    purity:
      template: allocating
    effects:
      template: pure
      may_throw: true
      allocates: true
      io: false
      network: false
      nondeterministic: false

  - name: JSON.stringify
    aliases: []
    semantics: JsonStringify
    signature: "(value) => string"
    since: "baseline"
    purity:
      template: pure
    effects:
      template: pure
      # Can throw on circular structures / BigInt, etc.
      may_throw: true
      allocates: true
      io: false
      network: false
      nondeterministic: false

