# Core ECMAScript Object APIs.

- name: Object.keys
  semantics: ObjectKeys
  signature: "(value) => string[]"
  since: "baseline"
  effects:
    template: pure
    may_throw: true
    allocates: true
    io: false
    network: false
    nondeterministic: false
  purity: Pure

- name: Object.values
  semantics: ObjectValues
  signature: "(value) => any[]"
  since: "baseline"
  effects:
    template: pure
    may_throw: true
    allocates: true
    io: false
    network: false
    nondeterministic: false
  purity: Pure

- name: Object.entries
  semantics: ObjectEntries
  signature: "(value) => [string, any][]"
  since: "baseline"
  effects:
    template: pure
    may_throw: true
    allocates: true
    io: false
    network: false
    nondeterministic: false
  purity: Pure

- name: Object.fromEntries
  semantics: ObjectFromEntries
  signature: "(iterable) => object"
  since: "baseline"
  effects:
    template: pure
    may_throw: true
    allocates: true
    io: false
    network: false
    nondeterministic: false
  purity:
    template: allocating

- name: Object.create
  semantics: ObjectCreate
  signature: "(proto, properties?) => object"
  since: "baseline"
  effects:
    template: pure
    may_throw: true
    allocates: true
    io: false
    network: false
    nondeterministic: false
  purity:
    template: allocating

- name: Object.is
  semantics: ObjectIs
  signature: "(value1, value2) => boolean"
  since: "baseline"
  effects: Pure
  purity: Pure

- name: Object.hasOwn
  semantics: ObjectHasOwn
  signature: "(object, propertyKey) => boolean"
  since: "baseline"
  effects:
    template: pure
    may_throw: true
    allocates: false
    io: false
    network: false
    nondeterministic: false
  purity: Pure
