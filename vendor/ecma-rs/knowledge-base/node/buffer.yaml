- name: node:buffer.Buffer.from
  aliases:
    - buffer.Buffer.from
    - Buffer.from
  since: "v5.10.0"
  effects:
    Custom:
      flags: ALLOCATES
      throws: Maybe
  purity: Impure
  async: false
  parallelizable: true
  idempotent: true
  deterministic: true

- name: node:buffer.Buffer.alloc
  aliases:
    - buffer.Buffer.alloc
    - Buffer.alloc
  since: "v5.10.0"
  effects:
    Custom:
      flags: ALLOCATES
      throws: Maybe
  purity: Impure
  async: false
  parallelizable: true
  idempotent: true
  deterministic: true

- name: node:buffer.Buffer.isBuffer
  aliases:
    - buffer.Buffer.isBuffer
    - Buffer.isBuffer
  since: "v0.1.0"
  effects: Pure
  purity: Pure
  async: false
  parallelizable: true
  idempotent: true
  deterministic: true

- name: node:buffer.Buffer.byteLength
  aliases:
    - buffer.Buffer.byteLength
    - Buffer.byteLength
  since: "v0.1.0"
  effects: Pure
  purity: Pure
  async: false
  parallelizable: true
  idempotent: true
  deterministic: true
