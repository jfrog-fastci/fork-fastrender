- name: node:http.request
  aliases:
    - http.request
  since: "v0.1.0"
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure
  async: true
  parallelizable: true
  idempotent: false
  deterministic: false

- name: node:http.get
  aliases:
    - http.get
  since: "v0.1.0"
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure
  async: true
  parallelizable: true
  idempotent: false
  deterministic: false

- name: node:http.createServer
  aliases:
    - http.createServer
  since: "v0.1.0"
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure
  async: false
  parallelizable: false
  idempotent: false
  deterministic: false
