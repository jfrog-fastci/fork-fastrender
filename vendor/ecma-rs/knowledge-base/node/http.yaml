- name: node:http.request
  aliases:
    - http.request
  since: "v0.1.0"
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure

- name: node:http.get
  aliases:
    - http.get
  since: "v0.1.0"
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure

- name: node:http.createServer
  aliases:
    - http.createServer
  since: "v0.1.0"
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure

- name: http.request
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure

- name: http.get
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure

- name: http.createServer
  effects:
    Custom:
      flags: IO | NETWORK
      throws: Maybe
  purity: Impure
