[package]
name = "runtime-native-abi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[build-dependencies]
# Pinned to keep the generated header stable and deterministic across builds.
cbindgen = "=0.26.0"
